import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'ADR Training - Quiz Hub',
  description: 'Interactive ADR knowledge training with quizzes, challenges and achievements'
}

export default function TrainingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}









